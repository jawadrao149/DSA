Name : Rao Jawad Ur Rehman.
REg.NO: 546040.

This is lab1 of DSA. There were 7 lab tasks and thus each of the task is saved in a seperate file such as lab1_taskx.cpp. to run any of these first build them with your compiler and then run the built file using ./lab1_taskx 
<!-- assume a prefix 'lab1_' with the name of each file below -->

task1.cpp creates an array named numbers containing 2, 4, 6, 8 and 10. Changes the third element to 7. Uses a for loop to display all five values, separated by spaces

task2.cpp reads an array from the user and then adds all the integers up in the array and displays the total.

task3.cpp defines a class named Student with two public integer data members, rollNumber and marks. Has a public member function void display() that prints both values with labels on separate lines. 

task4.cpp reads 8 integers into an array. Finds and displays the largest and smallest values and their indices. If a value occurs more than once, reports its first occurrence.

task6.cpp reads 6 elemnets from the user into an array annd reverses it and displays the reversed array.

task7.cpp reads 10 elements from the user and then takes all the unique elements and puts them at the start preserving the original ordfer and displays the unique elements and the total amount of unique elements.